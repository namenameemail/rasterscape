# После этапа 1 — буфер вне DOM

2026-09-08, 5.2 с. Сценарий как у [`00-before-1080.md`](00-before-1080.md): 1920×1080, видео берёт паттерн как source, маска включена, поверх рисуют кистью. Blur и DEPTH выключены.

Смысл замера: этап 1 не должен был ускорить кадр, только перенести пиксели с DOM-канваса. Проверяем, что `present` не съел выигрыш.

| Спан | count | avg ms | max ms |
|------|-------|--------|--------|
| `video.getFrameData` | 152 | **14.23** | 37.3 |
| `video.source.getImageData` | 152 | **14.20** | 37.3 |
| `video.pushNewFrame` | 152 | 1.68 | 4.0 |
| `video.shaderDraw` | 152 | 0.01 | 0.2 |
| `video.drawImage` | 152 | 1.99 | 9.9 |
| `canvas.present` | 210 | **0.50** | 3.8 |
| `values.updateMasked` | 21 | **41.47** | 47.5 |
| `draw.onDraw` | 56 | 14.62 | 47.8 |
| `draw.tool` | 56 | 0.23 | 0.5 |

`frame.interval`: avg **34.2 ms** (~29 FPS), max 183, min 16.6.

Сводка: [`01-after.session.json`](01-after.session.json).

## Что видно

- `canvas.present` — 0.5 ms на блит 1080p. Этап 1 обошёлся дёшево, кадр остался как в эталоне (34.2 против 32.5 ms).
- Кадр по-прежнему держат две вещи: `video.source.getImageData` **14.2 ms** (этап 2) и `values.updateMasked` **41.5 ms** раз в ~100 ms (этап 3).
- Шейдер 0.01 ms. Считает видеокарта мгновенно, всё время уходит на возню с пикселями в процессоре.
